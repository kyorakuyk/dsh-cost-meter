/**
 * Shared constants between the host and browser halves.
 *
 * @module dsh-cost-meter/constants
 */

/** HTTP route the Web client fetches for the cost overview (M3). */
export const OVERVIEW_ROUTE = '/cost-meter/api/overview'
